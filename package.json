{
  "name": "langtu",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "web:prepare": "node ./scripts/copy-web.mjs",
    "android:sync": "npm run web:prepare && npx cap sync",
    "android:copy": "npm run web:prepare && npx cap copy",
    "android:open": "npx cap open android",
    "android:assemble": "npm run web:prepare && npx cap sync && cd android && ./gradlew assembleDebug",
    "android:clean": "npx cap clean"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
